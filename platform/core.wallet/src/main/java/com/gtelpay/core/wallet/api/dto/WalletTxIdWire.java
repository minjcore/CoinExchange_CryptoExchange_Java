package com.gtelpay.core.wallet.api.dto;

/**
 * Wire fragment: payment/transfer result {@code data.walletTxId} (orchestration picks leg to expose).
 */
public record WalletTxIdWire(long walletTxId) {
}
