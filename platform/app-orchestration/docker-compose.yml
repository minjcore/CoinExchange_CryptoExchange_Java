services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gtelpay
      POSTGRES_USER: gtelpay
      POSTGRES_PASSWORD: gtelpay
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
