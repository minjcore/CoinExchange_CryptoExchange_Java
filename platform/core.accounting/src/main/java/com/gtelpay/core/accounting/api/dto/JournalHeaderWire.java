package com.gtelpay.core.accounting.api.dto;

/** Wire: {@code components/schemas/JournalHeader}. */
public record JournalHeaderWire(
        long id,
        String reference_id,
        String status) {
}
