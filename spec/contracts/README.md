# Wire contracts (canonical)

| Folder | Contents |
|--------|----------|
| [`open-api/`](open-api/) | `gtelpay-core-internal.yaml`, `gtelpay-public.yaml` (paymentorches), `accounting-internal.yaml`, `wallet-internal.yaml` |
| [`async-api/`](async-api/) | `core-events.yaml`, `core-commands.yaml` |
| [`gateway/`](gateway/) | `routes.example.yaml` |

Duplicates under `design/**/` kept for domain-local reading — **this tree is wire truth** ([ADR-018](../../adr/ADR-018-openapi-asyncapi-wire-truth.md)).

Root `open-api/`, `async-api/`, `gateway/` are redirect stubs only.
